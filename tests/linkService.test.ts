import LinkService from '../src/services/linkService';

describe('LinkService', () => {
  it('should be defined', () => {
    expect(LinkService).toBeDefined();
  });
  // Add more tests for createLink, getLink, etc.
});
